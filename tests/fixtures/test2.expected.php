<?php

namespace A;

func(\PREG_SPLIT_NO_EMPTY);

func(UNKNOWN);

func(TRUE, FALSE, NULL);

func(0,\PREG_SPLIT_NO_EMPTY,0);

func(0, \PREG_SPLIT_NO_EMPTY , 0);

func(\PREG_SPLIT_NO_EMPTY);

func(\ PREG_SPLIT_NO_EMPTY);

func(Type::PREG_SPLIT_NO_EMPTY);

func(Type :: PREG_SPLIT_NO_EMPTY);

func($obj::PREG_SPLIT_NO_EMPTY);

func($obj :: PREG_SPLIT_NO_EMPTY);

const
	PREG_SPLIT_NO_EMPTY = 1,
	PREG_SPLIT_DELIM_CAPTURE = 2;
